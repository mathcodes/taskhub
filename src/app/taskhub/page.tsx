import { Dashboard } from "@/components/Dashboard";

export const metadata = {
  title: "Task Hub — schedules, logs & AI",
  description:
    "Weekly tasks, completion logs, voice assistant, monitor and daily-summary agents.",
};

export default function TaskHubPage() {
  return <Dashboard />;
}
